# Исправление расчета учебного года для дат уроков

## 🐛 Проблема

При редактировании урока даты отображались с неправильным годом:
- **Было**: 12.01.2025
- **Должно быть**: 12.01.2026

## 🔍 Причина

Конвертация даты из формата `DD.MM` в формат `YYYY-MM-DD` для HTML date picker была жестко закодирована как:

```typescript
return `2025-${month.padStart(2, '0')}-${day.padStart(2, '0')}`;
```

Это не учитывало структуру **учебного года 2025-2026**:
- Сентябрь-Декабрь → 2025 год
- Январь-Август → 2026 год

## ✅ Решение

Добавлена функция `getAcademicYear()` для динамического определения года:

```typescript
const getAcademicYear = (monthNumber: string): number => {
  const month = parseInt(monthNumber, 10);
  // September (09) - December (12) = 2025
  if (month >= 9 && month <= 12) {
    return 2025;
  }
  // January (01) - August (08) = 2026
  return 2026;
};
```

### Использование:

```typescript
// Для даты урока
const year = getAcademicYear(month);
return `${year}-${month.padStart(2, '0')}-${day.padStart(2, '0')}`;

// Для даты уведомления
const year = getAcademicYear(month);
return `${year}-${month.padStart(2, '0')}-${day.padStart(2, '0')}`;
```

## 📝 Измененные файлы

- `frontend/src/components/LessonsSchedule.tsx`
  - Добавлена функция `getAcademicYear()`
  - Обновлена конвертация даты урока
  - Обновлена конвертация даты уведомления

## 🧪 Примеры работы

| Месяц | До исправления | После исправления |
|-------|---------------|-------------------|
| 12.09 | 2025-09-12 ✅ | 2025-09-12 ✅ |
| 15.10 | 2025-10-15 ✅ | 2025-10-15 ✅ |
| 20.11 | 2025-11-20 ✅ | 2025-11-20 ✅ |
| 25.12 | 2025-12-25 ✅ | 2025-12-25 ✅ |
| 12.01 | 2025-01-12 ❌ | 2026-01-12 ✅ |
| 14.02 | 2025-02-14 ❌ | 2026-02-14 ✅ |
| 10.03 | 2025-03-10 ❌ | 2026-03-10 ✅ |
| 05.04 | 2025-04-05 ❌ | 2026-04-05 ✅ |
| 20.05 | 2025-05-20 ❌ | 2026-05-20 ✅ |
| 15.06 | 2025-06-15 ❌ | 2026-06-15 ✅ |

## 🚀 Проверка

1. Откройте приложение
2. Перейдите в "Расписание и ДЗ"
3. Отредактируйте урок с датой в январе-августе
4. Проверьте, что date picker показывает правильный год (2026)

## 📊 Технические детали

- **Учебный год**: Сентябрь 2025 - Август 2026
- **Формат хранения**: `DD.MM` (без года)
- **Формат date picker**: `YYYY-MM-DD` (с годом)
- **Логика**: месяц >= 9 → 2025, месяц <= 8 → 2026

## 🔧 Если нужно изменить учебный год

Чтобы адаптировать на другой учебный год (например, 2026-2027), измените функцию:

```typescript
const getAcademicYear = (monthNumber: string): number => {
  const month = parseInt(monthNumber, 10);
  if (month >= 9 && month <= 12) {
    return 2026; // ← изменить год
  }
  return 2027; // ← изменить год
};
```

---

**Дата исправления**: 13 января 2026
**Статус**: ✅ Исправлено и протестировано
